import { PriorityBorderDirective } from './priority-border.directive';

describe('PriorityBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new PriorityBorderDirective();
    expect(directive).toBeTruthy();
  });
});
